const heading = document.querySelector("h1");
const but = document.querySelector("div .stat");
const para = document.querySelector(".area");
const all = document.querySelectorAll("h3");
console.log(heading, but, para, all);

